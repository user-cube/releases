# User Cube releases

Public, source-free distribution repository for User Cube products.

Each product owns its release-tag namespace. For example, Tackle publishes
`tackle-v0.2.2` with its CLI archives, signed desktop application and checksums.
The source repositories and their build pipelines remain private.
